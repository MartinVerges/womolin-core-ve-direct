{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug . && cmake --build ."
        }
    ],
    "version": "2.0.0"
}