{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "rm ve2mqtt; cmake -DCMAKE_BUILD_TYPE=Debug . && cmake --build ."
        }
    ],
    "version": "2.0.0"
}